# Truck Management System - CRUD
This is simple CRUD application is made covering the concept of Java Core and JDBC (using Maven).

# Sql Table
`CREATE TABLE trucks (
	id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50),
    model VARCHAR(50),
    capacity INT,
    driver_name VARCHAR(50)
);`
